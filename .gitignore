# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
env/
venv/
.venv/
*.egg-info/
dist/
build/
*.egg

# Node
node_modules/
.next/
out/
.env.local

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# Environment
.env
.env.local
.env.production

# Data
*.parquet
*.csv
*.db
*.db-shm
*.db-wal
*.db-journal
*.sqlite
*.duckdb

# ML Models
ml/saved_models/*.pkl
ml/saved_models/*.bin
ml/saved_models/*.safetensors

# Logs
*.log
logs/

# OS
.DS_Store
Thumbs.db

# Docker
docker-compose.override.yml
data/universe_cache.json
.alpha_checkpoints/
catboost_info/

# Generated data / debug scratch (not part of source)
scratch/
data/ledgers/

# mTLS certificates (NEVER commit private keys)
infrastructure/mtls/certs/

# Audit run json reports
audit/*.json

# OpenClaw agent workspace (not project source)
.openclaw/

# Agent memory files (OpenClaw runtime state)
memory/

# ALL pickle files (security risk — arbitrary code execution)
*.pkl
*.pkl.sha256

# Process ID files
*.pid

# Binary model artifacts (use MLflow artifact store instead)
models/*.pkl
models/*.pkl.sha256
ml/saved_models/**/*.pkl
ml/saved_models/**/*.pkl.sha256
ml/ranking_model/model.txt

# Audit reports with non-ASCII folder names
audit raporlar/

# Archived dead code (not production)
archive/
